server:
  port: 9002

spring:
  config:
    activate:
      on-profile: local

a2a:
  agent-url: http://localhost:9002

logging:
  level:
    io.github.cokelee777: DEBUG
    org.springframework.ai: DEBUG
